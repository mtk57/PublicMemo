自動化メニュー		
	ver.	1.3.0
		
		
	ブランチ作成	
		1.[Target]のVBプロジェクトファイル(C21～)を②のフォルダに収集する。
		"1.(B)に(A)をクローンする。
2.(D)にスイッチする。
3.(7)ブランチを作成する。
4.②のフォルダをコピーして(8)コミットする。(コピーは②\(C)配下を(B)\(C)にコピーするので注意)
5.(9)タグをつけてプッシュする。
6.2～5を[Target]の行数分繰り返す。"
		
	タグ収集	
		"1.(B)に(A)をクローンする。
2.(D)にスイッチする。
3.(9)タグをzipで②に出力する。
    →対象プロジェクトが参照するファイルのみを出力する場合は(G)を参照する。
4.3を[Target]の行数分繰り返す。"
		
	指定ブランチのHEADにタグ打ち	
		"1.(B)に(A)をクローンする。
2.(7)にスイッチする。
3.HEADに(9)タグをつけてプッシュする。
4.2～3を[Target]の行数分繰り返す。"
